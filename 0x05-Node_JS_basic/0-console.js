/**
 * Logs a message to the console.
 * and exports the displayMessage function
 */
const displayMessage = (message) => console.log(message);

/**
 * Exports the displayMessage function.
 */
module.exports = displayMessage;
